server:
  port:
    8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nayan-savi/spring-config-server
          search-paths:
            - catalog*
            - product*